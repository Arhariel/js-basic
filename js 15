const purchaseAmount = 15500; 
let discount = 0;
if (purchaseAmount > 1000) {
discount = 0.1;
} else if (purchaseAmount > 500) {
discount = 0.05;}
const totalAmount = purchaseAmount * (1 - discount);
console.log(`Сумма покупки с учетом скидки: ${totalAmount} грн`);